# Template for production backend / scripts (copy to `.env.production`, gitignored).
# Use the Supabase dashboard secret key (sb_secret_...) or legacy service_role JWT — never in VITE_*.

ENV=production
LOGURU_LEVEL=INFO

SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SECRET_KEY=

# Optional
# OPENROUTER_API_KEY=
