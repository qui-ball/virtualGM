---
phase: 03-tool-surface-consolidation-v2-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/tools.py
  - backend/game/models.py
  - backend/agent/definition.py
autonomous: true
requirements: [TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04, TOOLS-05, TOOLS-06, INV-01, INV-04, INV-05]
must_haves:
  truths:
    - "gm_agent registers <= 14 tools; remove_from_inventory, update_countdown, set_boss_battle are absent"
    - "Inventory add and remove both work through a single modify_inventory(action, item) tool"
    - "Countdown create and modify both work through a single set_countdown(name, value) upsert tool"
    - "Entering a boss battle via apply_damage(is_boss=True) still triggers the Blaze-of-Glory branch when the PC hits 0 HP"
    - "is_boss_battle auto-clears when the last enemy is removed, so a later non-boss fight does not wrongly fire Blaze-of-Glory"
    - "award_xp body is xp accounting + a call to a non-tool _check_level_up helper; the helper is not a registered tool"
    - "TOOLS-05 (implicit-LRU section caching) is documented as DEFERRED in ROADMAP.md and REQUIREMENTS.md per D-09; no eviction code is added this phase"
  artifacts:
    - path: "backend/agent/tools.py"
      provides: "merged modify_inventory + set_countdown tools, apply_damage is_boss arg, _check_level_up helper, set_boss_battle removed"
      contains: "def modify_inventory"
    - path: "backend/game/models.py"
      provides: "is_boss_battle field kept (unchanged shape)"
      contains: "is_boss_battle"
  key_links:
    - from: "backend/agent/tools.py apply_damage"
      to: "ctx.deps.is_boss_battle"
      via: "is_boss arg write path + existing PC-at-0 read path"
      pattern: "is_boss_battle"
    - from: "backend/agent/tools.py award_xp"
      to: "_check_level_up"
      via: "helper call appending level-up message"
      pattern: "_check_level_up"
---

<objective>
Tier 3 tool-surface consolidation in the live `backend/` stack. Merge the inventory pair into `modify_inventory(action, item)` (TOOLS-01), merge the countdown pair into `set_countdown(name, value)` upsert (TOOLS-02), retire `set_boss_battle` and move the flag write onto `apply_damage(is_boss=...)` with auto-clear on combat end (TOOLS-03), and factor level-up out of `award_xp` into a non-tool `_check_level_up` helper (TOOLS-04). Net result: 17 → 14 registered tools (TOOLS-06, target <= 14).

Purpose: Shrink the tool surface without dropping any in-fiction capability, keeping the SSE wire format byte-compatible (INV-01) and leaving `GameState`'s class shape, other Pydantic models, and `backend_generalist/` untouched.

Output: Edited `tools.py`, `models.py` (field kept; auto-clear side-effect lives in `remove_enemy` in tools.py), and a one-line `definition.py` prompt note for countdown-tick semantics.

NOTE: TOOLS-05 (implicit-LRU section caching) is DESCOPED this phase per D-09 — `load_campaign_section` / `unload_campaign_section` stay as-is. Do NOT touch them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tool-surface-consolidation-v2-0/03-CONTEXT.md
@.planning/phases/03-tool-surface-consolidation-v2-0/03-PATTERNS.md

@backend/agent/tools.py
@backend/game/models.py
@backend/agent/definition.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge inventory and countdown pairs into single tools</name>
  <files>backend/agent/tools.py</files>
  <behavior>
    - modify_inventory(action="add", item="sword"): appends item, returns updated inventory string; ModelRetry if pc is None (per D-03)
    - modify_inventory(action="remove", item="sword"): removes item, returns updated inventory string; ModelRetry if item not present (lists current inventory) and if pc is None (per D-03)
    - set_countdown(name="bomb", value=3): name absent -> creates at 3; name present -> sets to 3 absolute (upsert, per D-04)
    - set_countdown(name="bomb", value=0): reports "(TRIGGERED!)" in result string (per D-05)
    - set_countdown(name="bomb", value=-1): raises ModelRetry (per D-05)
  </behavior>
  <action>
    Replace `add_to_inventory` (tools.py:496-508) and `remove_from_inventory` (tools.py:511-528) with a single `modify_inventory(ctx, action: Literal["add","remove"], item: str)` tool per D-01/D-02/D-03. Add `from typing import Literal` to the import block (tools.py:1-20) — it is not currently imported in this file. Keep one `pc is None` guard shared by both actions; on `remove`, keep the not-present `ModelRetry` listing current inventory; both actions return the updated inventory in the result string. Document the `action` literal in the docstring (docstring is the single source of tool-shape doc per Phase-2 DEDUP-02).

    Replace `create_countdown` (tools.py:411-432) and `update_countdown` (tools.py:435-456) with a single `set_countdown(ctx, name: str, value: int)` tool using absolute-value upsert per D-04. Drop the create "already exists" ModelRetry and the update "not found" ModelRetry — upsert makes neither an error. Keep ONLY the `value < 0` -> ModelRetry guard (D-05). Assign absolutely (`ctx.deps.countdowns[name] = value`); no delta, no `max(0, ...)`. Report `value == 0` as "(TRIGGERED!)" in the result string (D-05). Preserve the in-place-mutate + emoji-log + return-string convention (Pattern C: keep the 🎒 and ⏱️ emojis).
  </action>
  <verify>
    <automated>cd backend && python -c "import agent.tools as t; assert hasattr(t,'modify_inventory'); assert hasattr(t,'set_countdown'); assert not hasattr(t,'add_to_inventory'); assert not hasattr(t,'remove_from_inventory'); assert not hasattr(t,'create_countdown'); assert not hasattr(t,'update_countdown'); print('ok')"</automated>
  </verify>
  <done>modify_inventory and set_countdown exist; the four merged-away tools no longer exist as module attrs; module imports cleanly with Literal imported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire set_boss_battle, add apply_damage is_boss arg + auto-clear, factor _check_level_up</name>
  <files>backend/agent/tools.py, backend/agent/definition.py</files>
  <behavior>
    - apply_damage(target="pc", amount=N, is_boss=True): sets ctx.deps.is_boss_battle=True; when PC hits 0 HP the result includes the "Blaze of Glory or Risk It All" branch (existing read path at tools.py:267-271 unchanged)
    - remove_enemy removing the last enemy: ctx.deps.is_boss_battle becomes False (D-08b auto-clear)
    - set_boss_battle no longer exists as a registered tool (D-07)
    - award_xp crossing a level threshold: result includes the LEVEL UP message via _check_level_up; below threshold: no level-up text
    - _check_level_up is NOT a registered @gm_agent.tool
  </behavior>
  <action>
    Delete the `set_boss_battle` tool entirely (tools.py:234-244) per D-07. Add an optional `is_boss: bool = False` arg to `apply_damage` (tools.py:247-295); near the top of the body, `if is_boss: ctx.deps.is_boss_battle = True` (D-08). Leave the PC-at-0-HP read branch (tools.py:267-271) exactly as-is — it is the Blaze-of-Glory read the flag exists for. Document `is_boss` in the docstring Args block (DEDUP-02). Keep the `GameState.is_boss_battle` field in models.py:145 unchanged (D-07; do NOT alter GameState's class shape — that is Phase 4).

    Add the auto-clear side-effect in `remove_enemy` (tools.py:217-231) per D-08b: after the `del ctx.deps.enemies[enemy_id]`, add `if not ctx.deps.enemies: ctx.deps.is_boss_battle = False`. This is the only structured combat-end signal in the live stack (no `in_combat` flip event exists — see PATTERNS "No Analog").

    Factor the level-up block (tools.py:481-491) out of `award_xp` into a module-level non-tool helper `_check_level_up(pc) -> str | None` per D-09a (place it just above `award_xp`, NO @gm_agent.tool decorator). The helper detects/applies a single level-up using `XP_THRESHOLDS` (already imported tools.py:11), logs the 🎉 line, and returns the player-facing message or None. `award_xp` keeps all three existing ModelRetry guards (pc is None, amount <= 0, in_combat) and after xp accounting calls `level_up_msg = _check_level_up(pc); if level_up_msg: result += level_up_msg`. Annotate the helper param as `pc: CharacterState` and add `CharacterState` to the `game.models` import (currently absent from tools.py:9-16).

    In `definition.py` instructions block (definition.py:74-128), add ONE terse line for countdown-tick semantics (D-06): to tick a countdown, call set_countdown with the new absolute value (one less than the current value shown in game state). Keep it to one line — rely on the tool docstring for shape (DEDUP-02). Do not enumerate tool args in the prompt.
  </action>
  <verify>
    <automated>cd backend && python -c "import inspect, agent.tools as t; assert not hasattr(t,'set_boss_battle'); assert hasattr(t,'_check_level_up'); assert 'is_boss' in inspect.signature(t.apply_damage).parameters; src=inspect.getsource(t.award_xp); assert '_check_level_up' in src; print('ok')" && python -c "import re; src=open('backend/agent/tools.py').read(); names=re.findall(r'@gm_agent\.tool(?:_plain)?\s*\n\s*def\s+(\w+)', src); assert len(names) <= 14, f'{len(names)} tools: {names}'; print(f'{len(names)} tools')"</automated>
  </verify>
  <done>set_boss_battle gone; apply_damage has is_boss param; remove_enemy clears the flag when enemies empty; _check_level_up exists and is uncalled by any decorator; award_xp delegates to it; registered tool count is <= 14; definition.py has the one-line countdown note.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md + REQUIREMENTS.md for the TOOLS-05 descope and corrected tool-count baseline</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>
    This task carries no code; it reconciles the planning docs with the decisions made for this phase so downstream verification does not flag TOOLS-05 / success-criterion #4 (automatic eviction) as a silent gap. Make these exact edits.

    In `.planning/ROADMAP.md`, Phase 3 section ONLY (do NOT touch Phase 1, 2, or 4 text):
    - Active-phases bullet (the `- [ ] **Phase 3: tool-surface-consolidation** —` line): change "(~15 → ≤11)" to "(17 → ≤14)" and strike the trailing "replace manual load/unload + 3-section cap with implicit LRU" clause (TOOLS-05 deferred; load/unload stay as-is this phase) per D-09/D-10.
    - Phase 3 **Goal** paragraph: change "registers ≤ 11 tools on `gm_agent` (down from ~15)" to "registers ≤ 14 tools on `gm_agent` (down from 17)" per D-10. Remove the implicit-LRU clause from the goal sentence (the "and replacing the manual `load_campaign_section` / `unload_campaign_section` pair + 3-section cap with implicit LRU caching" segment) so the goal no longer promises LRU work this phase.
    - Phase 3 **Requirements** list: on the TOOLS-05 line, prefix the description with "DEFERRED (per D-09): " (keep the line + ID for traceability; do not delete). On the TOOLS-06 line, change "≤ 11" to "≤ 14".
    - Phase 3 **Success Criteria** #1: change "≤ 11 tools registered" to "≤ 14 tools registered" and remove `unload_campaign_section` from the parenthetical "dropped tools" list (it is NOT dropped this phase).
    - Phase 3 **Success Criteria** #2: strike only the "loading a fourth distinct campaign section (forcing LRU eviction of the oldest)" clause so #2 no longer asserts LRU behavior; leave the rest of #2 intact.
    - Phase 3 **Success Criteria** #4 (the implicit-eviction criterion): prefix it with "DEFERRED (per D-09 — not required this phase): " so verification treats automatic eviction as out of scope here. Do not renumber other criteria.

    In `.planning/REQUIREMENTS.md`:
    - TOOLS-05 line (`- [ ] **TOOLS-05**:`): mark DEFERRED — prefix the body with "DEFERRED (see CONTEXT.md D-09 — manual load/unload retained this phase): ". Keep the ID and line for traceability.
    - TOOLS-06 line: change "≤ 11 (down from current 15)" to "≤ 14 (down from current 17)" per D-10.
    - Traceability table: update the TOOLS-05 row Status from "Pending" to "Deferred (D-09)".
    Do NOT touch DEDUP / STATE / INV rows or any v1.0 content.
  </action>
  <verify>
    <automated>python3 -c "rm=open('.planning/ROADMAP.md').read(); rq=open('.planning/REQUIREMENTS.md').read(); p3=rm[rm.index('### Phase 3:'):rm.index('### Phase 4:')]; assert '\u2264 11' not in p3, 'ROADMAP Phase 3 still says <= 11'; assert '\u2264 14' in p3, 'ROADMAP Phase 3 missing <= 14'; assert 'DEFERRED' in p3, 'ROADMAP Phase 3 missing TOOLS-05 DEFERRED'; t5=[l for l in rq.splitlines() if 'TOOLS-05' in l and 'DEFERRED' in l]; assert t5, 'REQUIREMENTS TOOLS-05 not marked DEFERRED'; t6=[l for l in rq.splitlines() if '**TOOLS-06**' in l]; assert t6 and '\u2264 14' in t6[0], 'REQUIREMENTS TOOLS-06 not <= 14'; print('docs ok')"</automated>
  </verify>
  <done>ROADMAP.md Phase 3 section no longer contains "≤ 11", states "≤ 14" for the goal/criterion #1/TOOLS-06, marks TOOLS-05 and success-criterion #4 as DEFERRED (per D-09), and no longer promises implicit-LRU work. REQUIREMENTS.md TOOLS-05 line contains "DEFERRED" with a D-09 pointer, TOOLS-06 contains "≤ 14", and the traceability TOOLS-05 row reads Deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM agent → tool args | The model supplies `action`, `item`, `name`, `value`, `is_boss`, `amount` — untrusted, validated via ModelRetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | set_countdown value | mitigate | Keep `value < 0` ModelRetry guard (D-05); absolute upsert removes delta/clamp ambiguity |
| T-03-02 | Tampering | modify_inventory remove | mitigate | Keep not-present ModelRetry listing current inventory (D-03); keep pc-None guard |
| T-03-03 | Elevation | stale is_boss_battle firing Blaze-of-Glory in a non-boss fight | mitigate | Auto-clear flag when last enemy removed (D-08b) |
| T-03-04 | Information disclosure | tool result strings | accept | Local CLI/single-user; no PII; result strings already returned today |
</threat_model>

<verification>
- `python -c "import agent.tools"` from backend/ imports without error.
- Registered tool count (count of `@gm_agent.tool`/`@gm_agent.tool_plain` decorators) is <= 14.
- Merged-away tools (`add_to_inventory`, `remove_from_inventory`, `create_countdown`, `update_countdown`, `set_boss_battle`) are absent; `modify_inventory`, `set_countdown` present; `apply_damage` has `is_boss`; `_check_level_up` is a non-tool helper.
- `git grep` confirms `is_boss_battle` is written via apply_damage (set True) and remove_enemy (set False when empty), and read in the PC-at-0 branch.
- No edits to `backend_generalist/` (INV-04), `frontend/`, `api/schemas.py`, or `GameState`'s class shape / other Pydantic models (INV-05).
</verification>

<success_criteria>
- 14 tools registered on gm_agent (down from 17), all named consolidations applied, no in-fiction capability dropped (TOOLS-06).
- modify_inventory covers add+remove (TOOLS-01); set_countdown covers create+modify via upsert (TOOLS-02); set_boss_battle retired with apply_damage(is_boss) write + auto-clear, Blaze-of-Glory branch intact (TOOLS-03); award_xp body is accounting + _check_level_up call, helper not registered (TOOLS-04).
- SSE event types and game_state payload field names unchanged (INV-01) — tool names do not appear in the wire format.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tool-surface-consolidation-v2-0/03-01-SUMMARY.md`
</output>
