---
phase: 03-tool-surface-consolidation-v2-0
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: false
requirements: [INV-01, INV-02, INV-03]
must_haves:
  truths:
    - "backend/cli.py starts a session and accepts a turn without crashing (INV-03 smoke)"
    - "A full turn through the React UI completes with the same SSE event types and payload field names as before the phase (INV-01/INV-02)"
    - "Every consolidation point is exercised end-to-end: inventory add+remove, countdown create+modify, boss battle Blaze-of-Glory, XP level-up, deferred dice roll round-trip"
  artifacts:
    - path: ".planning/phases/03-tool-surface-consolidation-v2-0/03-02-SUMMARY.md"
      provides: "human-verified golden-path smoke result"
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "backend SSE stream"
      via: "unchanged wire format"
      pattern: "game_state"
---

<objective>
End-of-phase golden-path smoke gate for Phase 3. Verify the consolidated tool surface from plan 01 still drives the live web UI and CLI with a byte-compatible SSE wire format, exercising every consolidation point in one play session.

Purpose: Phase 3's hard contract is INV-01 (SSE wire format byte-compatible); INV-02 (full turn incl. deferred dice roll completes) and INV-03 (CLI starts) are the smoke gates. This plan proves no in-fiction capability was dropped and the frontend needs zero code changes.

Output: Human-verified confirmation recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-tool-surface-consolidation-v2-0/03-CONTEXT.md
@.planning/phases/03-tool-surface-consolidation-v2-0/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI smoke + automated tool-surface assertions</name>
  <files></files>
  <action>
    Run the CLI smoke (INV-03): start `backend/cli.py`, confirm a session starts and at least one turn is accepted without crashing. Re-confirm the automated tool-surface assertions from plan 01: registered tool count <= 14; merged-away tools absent; modify_inventory / set_countdown / apply_damage(is_boss) / _check_level_up present. Confirm `backend_generalist/` and `frontend/` are untouched (INV-04) via `git status`. If any check fails, do not proceed to the human checkpoint — report the failure for a gap-closure pass.
  </action>
  <verify>
    <automated>cd backend && python -c "import re; src=open('agent/tools.py').read(); n=len(re.findall(r'@gm_agent\.tool(?:_plain)?\s*\n\s*def', src)); assert n <= 14, n; print(f'{n} tools')" && git -C .. status --porcelain backend_generalist frontend | (! grep .) && echo "INV-04/05 clean"</automated>
  </verify>
  <done>CLI starts and accepts a turn; tool-surface assertions pass; no edits leaked into backend_generalist/ or frontend/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The consolidated Tier 3 tool surface (modify_inventory, set_countdown, apply_damage(is_boss), _check_level_up, set_boss_battle retired) is wired into the live backend. The FastAPI app + React UI are unchanged.</what-built>
  <how-to-verify>
    1. Start the backend (FastAPI) and the React frontend as you normally do for the golden-path smoke.
    2. Play a single turn (or short sequence) through the UI that exercises every consolidation point:
       - Add an inventory item, then remove it.
       - Create a countdown, then modify it (tick it down via set_countdown to an absolute value).
       - Enter a boss battle (apply_damage with is_boss) and drive the PC to 0 HP — confirm the "Blaze of Glory / Risk It All" branch fires.
       - After the boss fight ends (last enemy removed), start a later NON-boss fight and drive the PC to 0 HP — confirm Blaze-of-Glory does NOT fire (validates the D-08b auto-clear of is_boss_battle).
       - Award XP that crosses a level-up threshold — confirm the LEVEL UP message appears.
       - Complete a deferred dice-roll round-trip (the dice-prompt UI).
    3. Confirm the SSE event types and payload field names look unchanged (same dice-prompt behavior, same game_state fields) and the frontend needed no code edits.
  </how-to-verify>
  <resume-signal>Type "approved" if the golden path completes with no frontend changes, or describe what broke.</resume-signal>
</task>

</tasks>

<verification>
- INV-03: CLI starts + accepts a turn.
- INV-01/INV-02: full turn (incl. deferred dice roll) completes through the UI with unchanged SSE event types and game_state payload field names; frontend code unchanged.
- All five consolidation points exercised end-to-end with no dropped capability.
</verification>

<success_criteria>
- Human-verified golden-path UI smoke passes with zero frontend code changes (INV-01/INV-02).
- CLI smoke passes (INV-03).
- Result recorded in 03-02-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tool-surface-consolidation-v2-0/03-02-SUMMARY.md`
</output>
