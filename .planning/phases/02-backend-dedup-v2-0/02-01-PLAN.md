---
phase: 02-backend-dedup-v2-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/turn_engine.py
  - backend/agent_test.py
  - backend/test_agent.py
autonomous: true
requirements: [DEDUP-01, DEDUP-04, INV-01, INV-02, INV-03, INV-05]

must_haves:
  truths:
    - "The SSE event-emission logic (queue creation, narrations.clear, _event_queue assignment, on_thinking closure, run_agent_iter call, _handle_result, finally cleanup + None sentinel, drain loop) appears exactly once in turn_engine.py"
    - "stream_turn and stream_deferred_response still exist as importable names, each with a body of ≤5 lines that delegates to the shared core"
    - "app.py and cli.py still import and call stream_turn / stream_deferred_response without modification"
    - "backend/agent_test.py and backend/test_agent.py no longer exist"
    - "_handle_result and _snapshot remain separate module-level helpers, unchanged"
  artifacts:
    - path: "backend/api/turn_engine.py"
      provides: "Single shared _stream_core helper plus two thin named entry-point wrappers"
      contains: "_stream_core"
    - path: "backend/api/turn_engine.py"
      provides: "Unchanged _snapshot and _handle_result helpers"
      contains: "def _handle_result"
  key_links:
    - from: "backend/api/turn_engine.py:stream_turn"
      to: "_stream_core"
      via: "delegation with runner_kwargs={'user_prompt': player_message}"
      pattern: "_stream_core"
    - from: "backend/api/turn_engine.py:stream_deferred_response"
      to: "_stream_core"
      via: "delegation with runner_kwargs containing message_history + deferred_tool_results"
      pattern: "_stream_core"
    - from: "backend/app.py"
      to: "backend/api/turn_engine.py"
      via: "from api.turn_engine import stream_deferred_response, stream_turn"
      pattern: "from api.turn_engine import"
---

<objective>
De-duplicate the two near-identical SSE turn-stream functions in `backend/api/turn_engine.py` into one shared private core, keeping `stream_turn` and `stream_deferred_response` as thin named wrappers. Also delete the two leftover scratch test files (`agent_test.py`, `test_agent.py`) that reference neither the GM agent nor any production module.

Purpose: Eliminate the duplicated event-emission logic (DEDUP-01) so the SSE code path exists in exactly one place, and remove dead scratch files from pytest's discovery radius (DEDUP-04) — all without changing import names, SSE event types, payload field names, or `GameStateSnapshot` (INV-01..05).
Output: A single `_stream_core` helper, two ≤5-line entry-point wrappers delegating to it, and two deleted files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-dedup-v2-0/02-CONTEXT.md

<interfaces>
<!-- Contracts the executor must preserve. Extracted from codebase. Use directly — no exploration needed. -->

Call sites that import the entry points (names MUST stay stable — INV-02/INV-03):
  backend/app.py:20  →  from api.turn_engine import stream_deferred_response, stream_turn
  backend/app.py:114 →  event_source = stream_deferred_response(session, result_str)
  backend/app.py:117 →  event_source = stream_turn(session, body.message)

Both entry points are async generators consumed via `async for event_type, data in event_source`.
Their signatures MUST remain:
  async def stream_turn(session: Session, player_message: str) -> AsyncGenerator[tuple[str, dict]]
  async def stream_deferred_response(session: Session, roll_result_str: str) -> AsyncGenerator[tuple[str, dict]]

The single agent invocation both paths wrap (from agent.runner):
  run_agent_iter(deps, message_history=..., user_prompt=... | deferred_tool_results=..., on_thinking=...)
  - stream_turn passes:               user_prompt=player_message, message_history=session.message_history
  - stream_deferred_response passes:  message_history=pending.messages_snapshot, deferred_tool_results=deferred_results

Helpers that MUST stay separate and unchanged (D-03):
  def _snapshot(session: Session) -> dict          # builds GameStateSnapshot(...).model_dump() — byte-identical, INV-05
  def _handle_result(session, result, queue)        # pushes "pending_action" or "complete" event onto queue
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Factor _stream_core and reduce the two entry points to thin wrappers</name>
  <read_first>
    - backend/api/turn_engine.py (the file being modified — both stream functions are byte-near-duplicates; the only difference is which runner kwargs are passed)
    - backend/app.py (confirm import line 20 and call sites lines 114/117 — names stream_turn / stream_deferred_response MUST NOT change)
    - backend/cli.py (confirm cli.py does NOT import the stream functions — it calls run_agent_iter directly; INV-03 smoke target)
  </read_first>
  <action>
    Add a private async generator helper `_stream_core(session: Session, runner_kwargs: dict[str, Any]) -> AsyncGenerator[tuple[str, dict]]` to turn_engine.py (per D-01). It owns ALL of the currently-duplicated shared body: queue creation (`asyncio.Queue[tuple[str, dict] | None]`), `gs = session.game_state`, `gs.narrations.clear()`, `gs._event_queue = queue`, the inner `run()` closure containing the `on_thinking` callback (logger.info + `queue.put_nowait(("thinking", {"text": text}))`), the `result = await run_agent_iter(deps=gs, on_thinking=on_thinking, **runner_kwargs)` call, the `_handle_result(session, result, queue)` call, the `except Exception` → `queue.put_nowait(("error", {"message": str(e)}))` branch, the `finally` → `gs._event_queue = None` + `queue.put_nowait(None)` sentinel, the `task = asyncio.create_task(run())`, the `while True: event = await queue.get(); if event is None: break; yield event` drain loop, and the trailing `await task`.

    Reduce `stream_turn(session, player_message)` (per D-02) to a body of ≤5 lines: build `runner_kwargs = {"message_history": session.message_history, "user_prompt": player_message}` and delegate via `async for event in _stream_core(session, runner_kwargs): yield event`. Keep its name, signature, docstring intent, and AsyncGenerator return type.

    Reduce `stream_deferred_response(session, roll_result_str)` (per D-02, D-04) to a body of ≤5 lines AFTER its deferred-specific preamble: keep the `if session.pending_deferred is None: raise ValueError("No pending deferred action on this session.")` precondition and the `DeferredToolResults()` assembly loop (`for call_info in session.pending_deferred.deferred_calls: deferred_results.calls[call_info["tool_call_id"]] = roll_result_str`) in the wrapper (D-04 — deferred-path-specific, not shared), then build `runner_kwargs = {"message_history": session.pending_deferred.messages_snapshot, "deferred_tool_results": deferred_results}` and delegate via `async for event in _stream_core(session, runner_kwargs): yield event`.

    Add `from typing import Any` to the imports. Do NOT touch `_snapshot` or `_handle_result` (D-03 — keep as separate module-level helpers, unchanged). Do NOT rename the entry points (app.py imports them — INV-02). Do NOT change SSE event types, payload keys, or `GameStateSnapshot` (INV-01, INV-05). Place `_stream_core` above the two wrappers; exact placement/ordering is Claude's discretion per CONTEXT.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast,sys; t=ast.parse(open('api/turn_engine.py').read()); fns={n.name:n for n in t.body if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef))}; assert '_stream_core' in fns, 'missing _stream_core'; assert '_handle_result' in fns and '_snapshot' in fns, 'helper removed'; st=fns['stream_turn']; sd=fns['stream_deferred_response']; assert (st.end_lineno-st.lineno)<=8, f'stream_turn body too long'; print('OK')" && python -c "import ast; src=open('api/turn_engine.py').read(); assert src.count('queue.put_nowait(None)')==1, 'sentinel not deduped'; assert src.count('gs._event_queue = queue')==1, 'event-queue assign not deduped'; assert src.count('while True:')==1, 'drain loop not deduped'; print('dedup OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && git grep -c 'gs._event_queue = queue' api/turn_engine.py` returns 1 (was 2 — shared body deduped).
    - `cd backend && git grep -c 'queue.put_nowait(None)' api/turn_engine.py` returns 1.
    - `cd backend && git grep -c 'def _stream_core' api/turn_engine.py` returns 1.
    - `cd backend && git grep -c 'def _handle_result' api/turn_engine.py` returns 1 and `git grep -c 'def _snapshot' api/turn_engine.py` returns 1 (helpers untouched, D-03).
    - `cd backend && python -c "import api.turn_engine as m; assert callable(m.stream_turn) and callable(m.stream_deferred_response)"` exits 0 (names preserved, INV-02).
    - `stream_deferred_response` still contains the `raise ValueError` precondition and `DeferredToolResults()` assembly (D-04): `cd backend && git grep -c 'DeferredToolResults()' api/turn_engine.py` returns 1.
  </acceptance_criteria>
  <done>turn_engine.py has one `_stream_core` containing the formerly-duplicated SSE body; `stream_turn` and `stream_deferred_response` are thin wrappers (≤5 lines of delegating body each) that still import and call cleanly from app.py; `_handle_result`/`_snapshot` unchanged; module imports without error.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the two leftover scratch test files</name>
  <read_first>
    - backend/agent_test.py (confirm it is the local-MLX-gemma chat REPL — references no GM agent / production module)
    - backend/test_agent.py (confirm it is the verbatim pydantic-ai weather-agent sample — references no GM agent / production module)
  </read_first>
  <action>
    Delete both `backend/agent_test.py` and `backend/test_agent.py` using `git rm` (per D-11). These are leftover scratch: `agent_test.py` is a local-MLX-gemma chat REPL, `test_agent.py` is the verbatim pydantic-ai weather-agent doc sample — neither imports the GM agent, its tools, or any production module. Deleting also removes the misleadingly-named `test_agent.py` from pytest's discovery radius. Do NOT touch `backend/test_tps.py` (D-12 — out of scope, intentional throughput benchmark).
  </action>
  <verify>
    <automated>cd backend && test ! -e agent_test.py && test ! -e test_agent.py && test -e test_tps.py && echo "OK: scratch deleted, test_tps preserved"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/agent_test.py` does not exist.
    - `backend/test_agent.py` does not exist.
    - `backend/test_tps.py` still exists (D-12 — untouched).
    - `cd backend && git grep -l 'agent_test\|test_agent' -- '*.py' | grep -v test_tps` returns nothing (no production module imported either deleted file).
  </acceptance_criteria>
  <done>Both scratch files removed via git; test_tps.py intact; no remaining import references to the deleted files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (frontend SSE) → FastAPI `/turns` | Untrusted player input; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | turn_engine.py SSE event stream | accept | No new attack surface — internal refactor only; SSE wire format (event types + payload field names) and player-input handling are unchanged (INV-01). No new endpoints, no new deserialization, no auth/serialization changes. |
</threat_model>

<verification>
- `cd backend && python -c "import api.turn_engine; import app"` imports both modules without error (proves wrappers + app call sites still resolve).
- `cd backend && git grep -c 'gs._event_queue = queue' api/turn_engine.py` returns 1.
- `backend/agent_test.py` and `backend/test_agent.py` are gone; `backend/test_tps.py` remains.
- SSE event types (`narration`, `thinking`, `pending_action`, `complete`, `error`) and payload keys are produced by `_handle_result`/`on_thinking`, which are unchanged — INV-01 holds structurally.
</verification>

<success_criteria>
- DEDUP-01: event-emission logic exists exactly once (`_stream_core`); `stream_turn` and `stream_deferred_response` are ≤5-line delegating wrappers with stable names/signatures.
- DEDUP-04: both `*_test_agent*.py` scratch files deleted; `test_tps.py` untouched.
- INV-02: app.py imports/call sites unchanged and resolve.
- INV-03: cli.py (which calls run_agent_iter directly, not the stream functions) is unaffected — confirmed in the end-of-phase smoke (Plan 02).
- INV-05: `_snapshot` / `GameStateSnapshot` unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-dedup-v2-0/02-01-SUMMARY.md`
</output>
