---
phase: 02-backend-dedup-v2-0
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/agent/definition.py
autonomous: false
requirements: [DEDUP-02, DEDUP-03, INV-01, INV-02, INV-03, INV-04, INV-05]

must_haves:
  truths:
    - "The system prompt in definition.py contains no per-tool signature/argument enumeration; tool-shape docs live only in @gm_agent.tool docstrings"
    - "The ## Output Format section references the narration tool generically, not by the literal name narrate(), while preserving the player-visible-text vs private-notes vs one-beat-per-turn rules"
    - "## Skill Checks (incl. the DC ladder), ## Combat Rules Summary, ## Core Responsibilities, ## GM Style, ## Pacing remain in the prompt"
    - "core-ruleset.md is read once at module load and embedded into the static instructions= string wrapped in <ruleset>...</ruleset>"
    - "The @gm_agent.instructions add_ruleset decorator is removed; add_campaign and current_game_state remain dynamic @gm_agent.instructions hooks"
    - "A full turn (including a deferred dice roll round-trip) plays through the React UI with the same SSE event sequence, and cli.py still starts a session and accepts a turn"
  artifacts:
    - path: "backend/agent/definition.py"
      provides: "Trimmed static prompt with statically-embedded ruleset constant"
      contains: "<ruleset>"
  key_links:
    - from: "backend/agent/definition.py module load"
      to: "backend/prompts/rulesets/core-ruleset.md"
      via: "Path(...).read_text() into a module-level constant, concatenated into instructions="
      pattern: "core-ruleset.md"
    - from: "backend/agent/definition.py:add_campaign"
      to: "@gm_agent.instructions"
      via: "still-decorated dynamic instruction hook"
      pattern: "@gm_agent.instructions"
    - from: "backend/agent/definition.py:current_game_state"
      to: "@gm_agent.instructions"
      via: "still-decorated dynamic instruction hook"
      pattern: "@gm_agent.instructions"
---

<objective>
Trim the GM system prompt in `backend/agent/definition.py` to remove the per-tool signature enumeration and the literal `narrate()` reference (DEDUP-02), and statically embed the core ruleset into the agent's `instructions=` string at module load while removing the dynamic `add_ruleset` hook (DEDUP-03). Then run the end-of-phase manual golden-path UI smoke gate covering both this plan's and Plan 01's changes (D-13/D-14, INV-01/INV-02/INV-03).

Purpose: Tool-shape documentation should live in exactly one place (`@gm_agent.tool` docstrings), and the static ruleset should not be re-read/re-injected per turn. Behavior, pacing, style, and mechanics coaching rules stay in the prompt verbatim — this is a surgical trim, not a rewrite.
Output: A trimmed `definition.py` with a module-level embedded-ruleset constant, two remaining dynamic instruction hooks, and a human-verified golden-path turn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-dedup-v2-0/02-CONTEXT.md

<interfaces>
<!-- Exact prompt structure to preserve/edit. Extracted from codebase. -->

definition.py current prompt sections (lines ~65-135 inside instructions="""..."""):
  ## Core Responsibilities   — KEEP verbatim (D-08)
  ## GM Style                — KEEP verbatim (D-08)
  ## Pacing                  — KEEP verbatim (D-08)
  ## Skill Checks            — KEEP verbatim incl. "easy 8, moderate 12, hard 15" DC ladder (D-07)
  ## Combat Rules Summary    — KEEP verbatim (D-07)
  ## Output Format           — REWRITE to de-name narrate() generically (D-06); preserve behavioral rules
  Tools: (lines ~115-132)    — DELETE the entire per-tool enumeration block (D-05)
  trailing "When your turn is complete, return your internal notes string." — KEEP

Current dynamic instruction hooks (lines ~144-216):
  @gm_agent.instructions def add_ruleset()          — REMOVE decorator + function entirely (D-09)
  @gm_agent.instructions def add_campaign(ctx)       — KEEP dynamic (D-10)
  @gm_agent.instructions def current_game_state(ctx) — KEEP dynamic (D-10)

add_ruleset currently does (this logic moves to module-load time, D-09):
  ruleset_path = Path(__file__).parent.parent / "prompts" / "rulesets" / "core-ruleset.md"
  content = ruleset_path.read_text(encoding="utf-8").strip()
  return f"<ruleset>\n{content}\n</ruleset>"

Tool docstrings already document tool shape (single source of truth after the Tools: block is deleted) — see @gm_agent.tool functions in backend/agent/tools.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed the ruleset statically at module load and remove the add_ruleset hook</name>
  <read_first>
    - backend/agent/definition.py (the file being modified — Agent(...) instructions= string lines ~65-136; the @gm_agent.instructions add_ruleset / add_campaign / current_game_state hooks lines ~144-216)
    - backend/prompts/rulesets/core-ruleset.md (the source-of-truth ruleset to embed; read-only this phase)
  </read_first>
  <action>
    Per D-09: at module-load time (above the `gm_agent = Agent(...)` construction), read `core-ruleset.md` once into a module-level constant. Reuse the exact path logic currently inside `add_ruleset`: `Path(__file__).parent.parent / "prompts" / "rulesets" / "core-ruleset.md"`, `.read_text(encoding="utf-8").strip()`, wrapped as `f"<ruleset>\n{content}\n</ruleset>"`. The constant name is Claude's discretion (e.g. `EMBEDDED_RULESET`).

    Concatenate this constant into the static `instructions=` string passed to `Agent(...)` — e.g. build the instructions as the existing prompt body + the embedded ruleset constant (an f-string or string concat). The ruleset must be part of the static instructions, not a per-run hook.

    Delete the `@gm_agent.instructions def add_ruleset()` decorator AND its function body entirely (D-09). Keep `@gm_agent.instructions def add_campaign(ctx)` and `@gm_agent.instructions def current_game_state(ctx)` exactly as-is — they depend on per-run `ctx.deps` and stay dynamic (D-10). Do not change any tool registration or SSE behavior (INV-01). Do not touch backend_generalist/ (INV-04).
  </action>
  <verify>
    <automated>cd backend && python -c "import ast; t=ast.parse(open('agent/definition.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef))}; assert 'add_ruleset' not in names, 'add_ruleset not removed'; assert 'add_campaign' in names and 'current_game_state' in names, 'dynamic hooks missing'; print('OK')" && python -c "import agent.definition as d; ins=d.gm_agent._instructions if hasattr(d.gm_agent,'_instructions') else ''; assert '<ruleset>' in d.gm_agent.__dict__.get('_instructions','') or any('<ruleset>' in str(v) for v in d.gm_agent.__dict__.values()) or True; import inspect; src=open('agent/definition.py').read(); assert 'core-ruleset.md' in src and src.count('@gm_agent.instructions')==2; print('embed+hooks OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -c 'def add_ruleset' agent/definition.py` returns 0 (D-09).
    - `cd backend && grep -c '@gm_agent.instructions' agent/definition.py` returns 2 (only add_campaign + current_game_state remain — D-10).
    - `cd backend && grep -c 'core-ruleset.md' agent/definition.py` returns 1 (read once, at module load).
    - `cd backend && grep -c '<ruleset>' agent/definition.py` returns ≥1 (ruleset wrapped + embedded in static instructions).
    - `cd backend && python -c "import agent.definition"` exits 0 (module loads, ruleset file readable at import time).
    - backend_generalist/ unmodified: `git status --porcelain backend_generalist/` returns nothing (INV-04).
  </acceptance_criteria>
  <done>core-ruleset.md is read once at module load into a constant and concatenated into the static `instructions=`; `add_ruleset` hook is gone; `add_campaign` and `current_game_state` remain dynamic; module imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Trim the system prompt — delete the Tools: enumeration and de-name the Output Format block</name>
  <read_first>
    - backend/agent/definition.py (the instructions= string — specifically the trailing `Tools:` enumeration block lines ~115-132 and the `## Output Format` section lines ~105-113)
    - backend/agent/tools.py (confirm @gm_agent.tool functions carry docstrings that document tool shape — these become the single source of tool documentation after the Tools: block is deleted, DEDUP-02)
  </read_first>
  <action>
    Per D-05: delete the entire `Tools:` enumeration block from the prompt (the bulleted per-tool signatures/argument lists: `load_campaign_section(...)`, `unload_campaign_section(...)`, `narrate(...)`, `roll_dice(...)`, `ask_player_roll(...)`, `create_enemy(...)`, `remove_enemy(...)`, `update_character_state(...)`, `set_boss_battle(...)`, `apply_damage(...)`, `apply_condition(...)`, `remove_condition(...)`, `award_xp(...)`, `add_to_inventory(...)`, `remove_from_inventory(...)`, `create_countdown(...)`, `update_countdown(...)`). Tool-shape documentation now lives only in `@gm_agent.tool` docstrings.

    Per D-06: rewrite the `## Output Format` section so it references the narration tool GENERICALLY (e.g. "route all player-visible text through the player-narration tool") instead of naming `narrate()` literally. Preserve the three behavioral rules verbatim in intent: (1) all player-visible text goes through narration; the final returned string is private notes never shown to the player; (2) the per-turn structure (state-management tool calls → narration → return a short private-notes string, never repeating narration, never including a player-facing prompt); (3) ONE story beat per turn (a beat may span multiple narration calls resolving a single action, but the story must not advance). Exact wording is Claude's discretion. Keep the trailing "When your turn is complete, return your internal notes string." line.

    Per D-07/D-08: KEEP `## Skill Checks` (including the "easy 8, moderate 12, hard 15" DC ladder and stat-matching bullets), `## Combat Rules Summary`, `## Core Responsibilities`, `## GM Style`, and `## Pacing` verbatim — these are GM adjudication coaching / behavior rules, NOT tool signatures, and are out of DEDUP-02 scope. Do not change SSE behavior or tool registration (INV-01).
  </action>
  <verify>
    <automated>cd backend && python -c "import agent.definition as d; src=open('agent/definition.py').read(); assert 'roll_dice(count' not in src and 'add_to_inventory(item)' not in src and 'set_boss_battle(active)' not in src, 'Tools: enumeration still present'; assert 'narrate()' not in src, 'narrate() still named literally in prompt'; assert '## Skill Checks' in src and 'moderate 12' in src and '## Combat Rules Summary' in src and '## Core Responsibilities' in src and '## GM Style' in src and '## Pacing' in src and '## Output Format' in src, 'a kept section was removed'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -c 'roll_dice(count' agent/definition.py` returns 0 and `grep -c 'add_to_inventory(item)' agent/definition.py` returns 0 (Tools: enumeration deleted — D-05).
    - `cd backend && grep -c 'narrate()' agent/definition.py` returns 0 (Output Format de-named — D-06).
    - `cd backend && grep -c '## Skill Checks' agent/definition.py` returns 1 and `grep -c 'moderate 12' agent/definition.py` returns 1 (DC ladder kept — D-07).
    - `cd backend && grep -c '## Combat Rules Summary' agent/definition.py` returns 1; `## Core Responsibilities`, `## GM Style`, `## Pacing`, `## Output Format` each grep-count 1 (kept — D-07/D-08).
    - `cd backend && python -c "import agent.definition"` exits 0.
  </acceptance_criteria>
  <done>The Tools: per-tool enumeration is gone; `## Output Format` no longer names `narrate()` literally but preserves the narration/private-notes/one-beat rules; all five kept sections remain verbatim; module imports cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase manual golden-path UI smoke (covers Plans 01 + 02)</name>
  <read_first>
    - backend/app.py (FastAPI routes that drive the SSE stream — confirm `uvicorn`/run entry)
    - backend/cli.py (INV-03 smoke target — session start + one turn)
  </read_first>
  <what-built>
    Plan 01 deduped the SSE turn-stream core (`_stream_core` + thin `stream_turn`/`stream_deferred_response` wrappers) and deleted two scratch test files. Plan 02 (Tasks 1-2) embedded the ruleset statically and trimmed the system prompt. This task is the single end-of-phase verification (D-14: smoke runs once at phase end, not per-commit) that the SSE wire format is byte-compatible and the deferred-dice-roll round-trip still works through the real UI (D-13: manual golden-path smoke, no automated SSE-fixture/diff harness — explicitly out of scope).
  </what-built>
  <how-to-verify>
    First, the automated INV-03 CLI quick-check (D-14 permits automating this):
    1. `cd backend && python -c "import app; import api.turn_engine; import agent.definition; print('imports OK')"` — all production modules import.
    2. Start `backend/cli.py`, confirm it prints the model/character startup banner and accepts at least one typed turn without crashing, then `exit`. (INV-03)

    Then the manual golden-path UI smoke (INV-01 + INV-02):
    3. Start the FastAPI app (e.g. `cd backend && uvicorn app:app --reload`) and the Vite dev server (frontend) per the project's usual run commands.
    4. In the React UI: create a session and play ONE turn whose narration triggers a player dice roll (a deferred `ask_player_roll`) — e.g. attempt something consequential so the GM asks for a roll.
    5. Confirm: narration text streams in (narration events), the dice-prompt UI appears (pending_action event), submitting the roll completes the turn (complete event), and game state renders correctly.
    6. Confirm the SSE event sequence and payload field names are unchanged vs before the phase — the React client (`frontend/src/api/client.ts`, `frontend/src/types/index.ts`) needed NO edits (INV-01). If the dice prompt renders and the turn completes without frontend changes, the wire format is intact.
  </how-to-verify>
  <acceptance_criteria>
    - `cd backend && python -c "import app"` exits 0 (production import graph resolves after both plans land).
    - `backend/cli.py` starts a session and accepts one turn without crashing (INV-03) — human confirms.
    - A full turn including a deferred dice roll round-trip completes through the React UI (INV-02) — human confirms.
    - SSE event types (`narration`, `thinking`, `pending_action`, `complete`, `error`) and payload field names are unchanged; no edits to `frontend/` were required (INV-01) — human confirms.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the CLI smoke passes and the golden-path turn (including the deferred dice roll) completes in the React UI with no frontend changes; otherwise describe what broke (which event/payload differed, or where the turn failed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (frontend SSE) → FastAPI `/turns` | Untrusted player input; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | agent system prompt (definition.py) | accept | No new attack surface — prompt trim and static ruleset embed change only the LLM instructions, not input handling, endpoints, or serialization. Ruleset content is the same data previously injected per-turn, now embedded once. SSE wire format unchanged (INV-01). |
</threat_model>

<verification>
- `cd backend && python -c "import agent.definition; import app"` imports cleanly (ruleset readable at import time, app graph resolves).
- `grep -c '@gm_agent.instructions' backend/agent/definition.py` returns 2; `grep -c 'def add_ruleset' backend/agent/definition.py` returns 0.
- Tools: enumeration and literal `narrate()` removed; all five kept prompt sections present.
- Human-verified: CLI smoke (INV-03) + golden-path deferred-dice-roll turn through React UI with no frontend edits (INV-01/INV-02).
</verification>

<success_criteria>
- DEDUP-02: prompt has no per-tool signature enumeration; tool docs live only in `@gm_agent.tool` docstrings; behavior/pacing/style/mechanics sections retained.
- DEDUP-03: ruleset read once at module load, embedded in static `instructions=`; `add_ruleset` removed; `add_campaign` + `current_game_state` remain dynamic.
- INV-01/INV-02/INV-03: golden-path UI smoke + CLI smoke pass with byte-compatible SSE and no frontend edits.
- INV-04: backend_generalist/ untouched. INV-05: no Pydantic-model shape changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-dedup-v2-0/02-02-SUMMARY.md`
</output>
