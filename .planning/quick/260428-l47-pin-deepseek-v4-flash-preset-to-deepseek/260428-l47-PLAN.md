---
phase: 260428-l47
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/definition.py
  - backend_generalist/agent.py
autonomous: true
requirements:
  - QUICK-260428-l47
must_haves:
  truths:
    - "deepseek-v4-flash preset in backend/agent/definition.py pins provider order to ['deepseek']"
    - "deepseek-v4-flash preset in backend_generalist/agent.py pins provider order to ['deepseek']"
    - "Existing 'deepseek' (v3.2) preset remains untouched in both files"
    - "DEFAULT_MODEL remains 'qwen3.5' in both files"
    - "backend_generalist tests still pass 38/38"
  artifacts:
    - path: "backend/agent/definition.py"
      provides: "MODEL_PRESETS['deepseek-v4-flash'] -> ('deepseek/deepseek-v4-flash', 'deepseek')"
      contains: '"deepseek-v4-flash": ("deepseek/deepseek-v4-flash", "deepseek")'
    - path: "backend_generalist/agent.py"
      provides: "DEFAULT_MODEL_PRESETS['deepseek-v4-flash'] -> ('deepseek/deepseek-v4-flash', 'deepseek')"
      contains: '"deepseek-v4-flash": ("deepseek/deepseek-v4-flash", "deepseek")'
  key_links:
    - from: "MODEL_PRESETS['deepseek-v4-flash'][1]"
      to: "OpenRouterModelSettings.openrouter_provider.order"
      via: "build_model_settings splits provider string on ',' and assigns to order"
      pattern: 'p\.strip\(\) for p in provider\.split\(","\)'
---

<objective>
Pin the `deepseek-v4-flash` preset to DeepSeek's first-party OpenRouter endpoint by changing the provider field from empty string to `"deepseek"` in both `backend/agent/definition.py` (`MODEL_PRESETS`) and `backend_generalist/agent.py` (`DEFAULT_MODEL_PRESETS`).

Purpose: Avoid AkashML 502 errors observed after commit `8f0a852`. DeepSeek's first-party endpoint reports 100% uptime per OpenRouter's endpoints API; pinning the provider order skips AkashML entirely while preserving fallback behavior (`allow_fallbacks: True` is already wired in both `build_model_settings`/`_build_model_settings`).

Output: Two single-line edits — one per file. No new code, no new tests, no signature changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Exact strings to find and replace. Verified against the live files at plan time. -->
<!-- Both files have IDENTICAL target lines — same substitution applied to each. -->

backend/agent/definition.py — line 17 (inside `MODEL_PRESETS` dict, lines 14–24):

  Before:
      "deepseek-v4-flash": ("deepseek/deepseek-v4-flash", ""),
  After:
      "deepseek-v4-flash": ("deepseek/deepseek-v4-flash", "deepseek"),

backend_generalist/agent.py — line 116 (inside `DEFAULT_MODEL_PRESETS` dict, lines 112–119):

  Before:
      "deepseek-v4-flash": ("deepseek/deepseek-v4-flash", ""),
  After:
      "deepseek-v4-flash": ("deepseek/deepseek-v4-flash", "deepseek"),

DO NOT touch the line directly above in either file:
      "deepseek": ("deepseek/deepseek-v3.2", ""),     # remains as-is

Plumbing already in place — confirm only, do NOT modify:

  backend/agent/definition.py: build_model_settings (lines 39–47):
      def build_model_settings(provider: str) -> OpenRouterModelSettings:
          if not provider:
              return OpenRouterModelSettings()
          return OpenRouterModelSettings(
              openrouter_provider={
                  "order": [p.strip() for p in provider.split(",")],
                  "allow_fallbacks": True,
              }
          )

  backend_generalist/agent.py: _build_model_settings (lines 123–132):
      Same shape — splits provider on ',' into `openrouter_provider.order`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin deepseek-v4-flash preset to DeepSeek provider in both modules</name>
  <files>backend/agent/definition.py, backend_generalist/agent.py</files>
  <action>
Make the EXACT same substitution in both files using the Edit tool. Two single-line edits total.

1. Edit `backend/agent/definition.py`:
   - Find:    `"deepseek-v4-flash": ("deepseek/deepseek-v4-flash", ""),`
   - Replace: `"deepseek-v4-flash": ("deepseek/deepseek-v4-flash", "deepseek"),`
   - This line lives in the `MODEL_PRESETS` dict (around line 17).

2. Edit `backend_generalist/agent.py`:
   - Find:    `"deepseek-v4-flash": ("deepseek/deepseek-v4-flash", ""),`
   - Replace: `"deepseek-v4-flash": ("deepseek/deepseek-v4-flash", "deepseek"),`
   - This line lives in the `DEFAULT_MODEL_PRESETS` dict (around line 116).

DO NOT:
- Modify the `"deepseek": ("deepseek/deepseek-v3.2", ""),` entry in either file (the line directly above the target).
- Modify `build_model_settings` or `_build_model_settings` — the comma-split → `openrouter_provider.order` plumbing is already correct.
- Change `DEFAULT_MODEL` in either file (must remain `"qwen3.5"`).
- Add tests, change other presets, or touch any other files.

Rationale (per task_specifics): AkashML returned 502 `Backend request failed` for this model post-commit `8f0a852`. DeepSeek's first-party endpoint has 100% uptime per OpenRouter's endpoints API. Pinning `order: ["deepseek"]` with `allow_fallbacks: True` (already wired) routes to the healthy provider while still allowing fallback if needed.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from agent.definition import MODEL_PRESETS; assert MODEL_PRESETS['deepseek-v4-flash'] == ('deepseek/deepseek-v4-flash', 'deepseek'), MODEL_PRESETS['deepseek-v4-flash']; assert MODEL_PRESETS['deepseek'] == ('deepseek/deepseek-v3.2', ''), MODEL_PRESETS['deepseek']; print('backend OK:', MODEL_PRESETS['deepseek-v4-flash'])" && cd ../backend_generalist && uv run python -c "from backend_generalist.agent import DEFAULT_MODEL_PRESETS, DEFAULT_MODEL; assert DEFAULT_MODEL_PRESETS['deepseek-v4-flash'] == ('deepseek/deepseek-v4-flash', 'deepseek'), DEFAULT_MODEL_PRESETS['deepseek-v4-flash']; assert DEFAULT_MODEL_PRESETS['deepseek'] == ('deepseek/deepseek-v3.2', ''), DEFAULT_MODEL_PRESETS['deepseek']; assert DEFAULT_MODEL == 'qwen3.5', DEFAULT_MODEL; print('generalist OK:', DEFAULT_MODEL_PRESETS['deepseek-v4-flash'])" && cd .. && uv run --directory backend_generalist pytest tests -q</automated>
  </verify>
  <done>
- `MODEL_PRESETS['deepseek-v4-flash']` in `backend/agent/definition.py` equals `('deepseek/deepseek-v4-flash', 'deepseek')`.
- `DEFAULT_MODEL_PRESETS['deepseek-v4-flash']` in `backend_generalist/agent.py` equals `('deepseek/deepseek-v4-flash', 'deepseek')`.
- The existing `deepseek` (v3.2) preset is unchanged in both files (still `("deepseek/deepseek-v3.2", "")`).
- `DEFAULT_MODEL` in both files is unchanged (`"qwen3.5"`).
- `backend_generalist` test suite passes 38/38.
- No other files modified.
  </done>
</task>

</tasks>

<verification>
- Import-time tuple equality assertions for both modules (see task verify command).
- Negative-control assertions confirm the v3.2 `deepseek` preset and `DEFAULT_MODEL` are untouched.
- Full `backend_generalist/tests` pytest run remains green (38/38).
- Optional manual sanity: `git diff backend/agent/definition.py backend_generalist/agent.py` should show exactly two lines changed (one per file), each adding `deepseek` inside the second tuple slot.
</verification>

<success_criteria>
- Two single-line edits applied; no other code or test changes.
- All assertions in the automated verify command pass.
- pytest reports 38 passed, 0 failed.
- Commit message describes provider pin to fix AkashML 502.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-l47-pin-deepseek-v4-flash-preset-to-deepseek/260428-l47-01-SUMMARY.md` documenting the two-line change and verification result.
</output>
