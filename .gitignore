# Environment files (keep .env.example as template; .env.docker has safe defaults and is committed)
.env
.env.*
!.env.example
!.env.docker
__pycache__/
.claude
local/

# Launch script runtime (frontend PID and log)
.launch-frontend.pid
.launch-frontend.log

# Recorded session data (potentially large)
backend/recordings/

# Per-session world directories created by backend_generalist CLI
/sessions/

# macOS metadata
.DS_Store