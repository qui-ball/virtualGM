# Environment files: .env.example = template; .env.docker = Compose defaults (prefer local secrets in gitignored .env.development)
.env
.env.*
!.env.example
!.env.docker
__pycache__/
.claude
local/

# Launch script runtime (frontend PID and log)
.launch-frontend.pid
.launch-frontend.log

# Editor — keep local only
.vscode/