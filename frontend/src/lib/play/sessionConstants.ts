/** Phase 1 pull reveal: character name / XP header (px). */
export const PHASE1_MAX = 90;

/** Fallback phase-2 height when layout has not been measured yet. */
export const FALLBACK_PHASE2_MAX = 380;

export const FALLBACK_FULL_PULL = PHASE1_MAX + FALLBACK_PHASE2_MAX;
