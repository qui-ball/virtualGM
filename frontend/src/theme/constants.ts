/** localStorage key for guest theme persistence (see design.md / Task 7). */
export const THEME_STORAGE_KEY = 'virtualgm-ui-theme';

/** Injected stylesheet links carry this attribute so we dedupe per theme. */
export const FONT_LINK_DATA_ATTR = 'data-virtualgm-fonts';
